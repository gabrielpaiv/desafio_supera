import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: '6%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  gameImg: {
    width: 175,
    height: 175,
  },
  gameTitle: {
    fontSize: 16,
  },
  gamePrice: {
    fontSize: 16,    
  }
});